Index: retroarch/menu/drivers/menu_generic.c
===================================================================
--- retroarch.orig/menu/drivers/menu_generic.c
+++ retroarch/menu/drivers/menu_generic.c
@@ -396,7 +396,7 @@ int generic_menu_entry_action(
                   entry->label, entry->type, i, entry->entry_idx);
          break;
       case MENU_ACTION_SEARCH:
-         menu_input_dialog_start_search();
+         //menu_input_dialog_start_search();
          break;
       case MENU_ACTION_SCAN:
          if (cbs && cbs->action_scan)
